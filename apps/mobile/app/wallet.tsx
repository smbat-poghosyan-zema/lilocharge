import { WalletScreen } from '../src/features/wallet/wallet-screen';

/**
 * Mounts the wallet balance, top-up, and transactions screen.
 */
export default function WalletRoute(): JSX.Element {
  return <WalletScreen />;
}
