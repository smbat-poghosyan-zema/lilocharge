import { StationDetailScreen } from '../../src/features/stations/station-detail-screen';

/**
 * Mounts the full station detail screen for one station id.
 */
export default function StationDetailRoute(): JSX.Element {
  return <StationDetailScreen />;
}
