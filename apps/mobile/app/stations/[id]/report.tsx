import { ProblemReportScreen } from '../../../src/features/problem-reports/problem-report-screen';

/**
 * Mounts the station problem-report form for one station id.
 */
export default function StationProblemReportRoute(): JSX.Element {
  return <ProblemReportScreen />;
}
