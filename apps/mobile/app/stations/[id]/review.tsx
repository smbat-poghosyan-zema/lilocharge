import { ReviewFormScreen } from '../../../src/features/reviews/review-form-screen';

/**
 * Mounts the station review form for one station id.
 */
export default function StationReviewRoute(): JSX.Element {
  return <ReviewFormScreen />;
}
