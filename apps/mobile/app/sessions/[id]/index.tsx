import { ActiveSessionScreen } from '../../../src/features/sessions/active-session-screen';

/**
 * Mounts the live charging session screen for one session id.
 */
export default function ActiveSessionRoute(): JSX.Element {
  return <ActiveSessionScreen />;
}
