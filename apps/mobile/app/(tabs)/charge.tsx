import { ScanScreen } from '../../src/features/sessions/scan-screen';

/**
 * Mounts the QR scan screen as the central "Charge" tab.
 */
export default function ChargeTabRoute(): JSX.Element {
  return <ScanScreen />;
}
