import { ChargeConfirmScreen } from '../../src/features/sessions/charge-confirm-screen';

/**
 * Mounts the charge confirmation screen for a scanned connector.
 */
export default function ChargeConfirmRoute(): JSX.Element {
  return <ChargeConfirmScreen />;
}
